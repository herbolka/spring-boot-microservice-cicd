apiVersion: v1
kind: Service
metadata:
  name: currency-conversion-service
  labels:
    app: currency-conversion
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: currency-conversion